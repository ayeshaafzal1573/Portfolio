# Legacy-to-be-replaced admin editors + stock shadcn/ui primitives.
# These will be superseded by the WordPress-style admin panel (audit block B).
# Deliberately scoped: public portfolio components, lib/config.ts and
# lib/useConfig.ts are linted and type-checked in full.
components/admin/
components/ui/
